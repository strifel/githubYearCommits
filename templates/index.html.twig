<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Leaderboard of Contributions</title>

    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.bundle.min.js" rel="stylesheet">
</head>
<body data-affect-dark-mode="!background-color">

<h1 data-affect-dark-mode="color">Contributions {{ timespan }}</h1>
<table>
    {% for user in users %}
        <tr class="contributionUser">
            <td class="usernameTD"><a class="username" href="/participant/{{ user.name }}">{{ user.name }}</a></td>
            <td class="contributionsTD" data-affect-dark-mode="color"><a class="contributions">{{ user.contributions }}</a></td>
            <td class="percentageTD" data-affect-dark-mode="color"><a class="percentage"></a></td>
        </tr>
    {% endfor %}
</table>

<button onclick="changeDarkMode(true)" id="darkModeButton">Dark Mode</button>

<script src="/static/percentage.js"></script>
<script src="/static/darkmode.js"></script>
</body>
</html>
<style>
    .percentageTD {
        border-left: 0;
        padding-left: 0;
    }

    .percentage {
        font-size: 12px;
    }

    .username {
        font-weight: bold;
        font-size: 25px;
    }

    .contributionsTD {
        padding-right: 0;
        border-right: 0;
    }

    .contributions {
        text-align: right;
        font-weight: bold;
        font-size: 25px;
    }

    td {
        text-align: left;
        padding-right: 30px;
        padding-top: 10px;
        padding-bottom: 10px;
    }

    table {
        margin: 0 auto;
    }

    h1 {
        margin: auto;
        text-align: center;
        padding-bottom: 30px;
        padding-top: 10px;
    }

    button {
        background-color: #1D1F21;
        color: #f5f5f5;

        border: none;
        border-radius: 5px;

        right: 1%;
        bottom: 1%;
        position:absolute;
    }
</style>